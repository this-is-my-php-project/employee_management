{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "db-reset": "php artisan migrate:fresh && php artisan db:seed && php artisan passport:install --force && php artisan cache:clear"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
